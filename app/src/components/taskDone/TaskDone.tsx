import React, {FC} from 'react';
import {TaskDoneProps} from "./TaskDoneProps";
import {View} from "react-native";

const TaskDone: FC<TaskDoneProps> = ({}): JSX.Element => {
    return (
        <View>

        </View>
    );
};

export default TaskDone;
